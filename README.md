# Setup
Install node dependencies run `npm install`

# To run app in dev mode
To start App in dev mode run `npm run start:dev`

# To run app in production mode
First build the application using `npm run build`. After building application run `npm start` to run the app

# ENV
Application takes following env variables
1. NUMBER_OF_CONSECUTIVE_DAYS `number of consecutive days to get repeated customers`
